import flet as ft

class MyButton(ft.ElevatedButton)
    def __init__(self, text):
        super().__init__()
        self.bgcolor = ft.Colors.ORANGE_300
        self.color = ft.Colors.GREEN_800
        self.text = text  

button = MyButton()
print(type(button) == ft.ElevatedButton,type(button) == MyButton
,isinstance(button, ft.ElevatedButton)
 ,isinstance(button, MyButton